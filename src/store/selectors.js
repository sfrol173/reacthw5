

export const selectorMenCards = (state) => state.men.goods;

export const selectorFavoriteList = (state) => state.app.favoriteList;

export const selectorCartList = (state) => state.app.cartList;

export const selectorTotalPrice = (state) => state.app.totalPrice;

export const selectorUserInfo = (state) => state.app.userInfo;

export const selectorFavoriteNum = (state) => state.app.favoriteNum;

export const selectorCartNum = (state) => state.app.cartNum;


